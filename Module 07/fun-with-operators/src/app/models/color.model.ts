export interface Color {
    readonly name: string;
    readonly code: string;
}

// when you create an object using "new", define it as a class
// when you create an object using JSON, define it as an interface